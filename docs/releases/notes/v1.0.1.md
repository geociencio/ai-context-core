# Release v1.0.1 - 2026-01-22

## 🛠 Fixes

### Configuration
- **Fixed missing configuration files (`defaults.yaml`)** in the installed package.
- Added `MANIFEST.in` to explicitly include non-Python data files (`.yaml`, `.json`, `.md`, `.txt`).
- Updated `pyproject.toml` to enable `include-package-data`.

## 📦 Installation
```bash
uv pip install ai-context-core==1.0.1
```
